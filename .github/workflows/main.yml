name: jobs

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "first job"

  second-job:
    runs-on: ubuntu-latest
    needs: first-job  
    steps:
      - name: Say goodbye
        run: echo " second job, depending on the first one"
